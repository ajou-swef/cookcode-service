import XCTest
@testable import cookcode_service

final class cookcode_serviceTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(cookcode_service().text, "Hello, World!")
    }
}
